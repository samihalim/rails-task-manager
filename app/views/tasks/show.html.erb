<h1><%= @task.title %></h1>

<p><input type="checkbox"><%= @task.details %></p>
<p><input type="checkbox">Completed<%= @task.completed%></p>


<p>
  <%= link_to 'Delete', task_path,
                        method: :delete,
                        data: { confirm: 'Are you sure?' } %>
</p>

<p><%= link_to 'Go back to Tasks', tasks_path%></p>
<p><%= link_to 'Update tasks', edit_task_path%></p>
